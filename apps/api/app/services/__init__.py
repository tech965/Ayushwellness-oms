"""Business logic layer.

Status: PLANNED — populated alongside each module starting Phase 1.

Routes call services; services call repositories. Business rules never
live in API route handlers (see docs/architecture/backend.md).
"""
