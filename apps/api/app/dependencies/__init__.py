"""Shared FastAPI dependencies (auth, pagination, RBAC guards).

Status: PLANNED — Phase 1 introduces get_current_user and
require_permission() dependencies once the User/Role/Permission models
exist.
"""
