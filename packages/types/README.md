# @ayushwellness/types

Status: PLANNED.

Shared TypeScript types generated/derived from the backend's Pydantic schemas (orders, shipments, customers, ...) so `apps/web` never hand-duplicates the API contract. Introduced once the Phase 1 domain models stabilize.
